<h1>Wikipages</h1>

<div class="col-md-8">
  <h1>Contacts</h1>
  <ul>
  {{#each itemController='contact'}}
    <li>{{#link-to 'contact' this}}{{name}}{{/link-to}}
        <button {{action deleteContact}} class="btn btn-danger btn-xs">Delete</button></li>
  {{/each}}
  </ul>
</div>

<div class="col-md-4">
  {{outlet}}
</div>
